import 'package:flutter/material.dart';

// Shared styling and spacing used across learn screens
class LearnStyles {
  static const double borderRadius = 12.0;
  static final Color cardBackground = const Color(0xFFF8F5FA);
  static final Color pageBackground = const Color(0xFFF6F7FB);
  static final EdgeInsets pagePadding = const EdgeInsets.symmetric(
    horizontal: 24,
    vertical: 20,
  );
}
